# Настройка Resend для отправки Email

## Почему Resend?

**Resend** - современный email-сервис с действительно бесплатным планом:

✅ **100 emails/день БЕСПЛАТНО (навсегда)** - без триалов и ограничений
✅ **Простая регистрация** - буквально 2 минуты
✅ **Современный API** - проще чем SendGrid
✅ **Работает на облачных платформах** - использует API, не SMTP
✅ **Без кредитной карты** - не требуется для бесплатного плана

---

## Быстрая настройка (5 минут)

### Шаг 1: Регистрация

1. Перейдите на https://resend.com/signup
2. Зарегистрируйтесь через GitHub или email
3. Подтвердите email (если регистрировались через email)

### Шаг 2: Получение API ключа

1. После входа откроется Dashboard
2. Нажмите **API Keys** в левом меню
3. Нажмите **Create API Key**
4. Введите название: `Anatomia Production`
5. Permissions: оставьте **Sending access** (по умолчанию)
6. Domain: оставьте **All domains**
7. Нажмите **Add**
8. **ВАЖНО**: Скопируйте API ключ!
   ```
   re_gp532ndX_AvZox7NLgm24UBFFGfXcXj1i
   ```
   Ключ показывается только один раз!

### Шаг 3: Добавление домена (опционально, но рекомендуется)

**Вариант A: Использовать email Resend (быстро, для тестирования)**

Можете пропустить этот шаг и использовать:
```
onboarding@resend.dev
```

Emails будут приходить, но от имени Resend. Это OK для тестирования!

**Вариант B: Добавить свой домен (для production)**

#### 1. Добавление домена в Resend

1. В Dashboard: **Domains** → **Add Domain**
2. Введите ваш домен: `anatomia.md` (или ваш домен)
3. Нажмите **Add**
4. Resend покажет DNS записи, которые нужно добавить

#### 2. Добавление DNS записей

**ВАЖНО**: DNS записи нужно добавить у вашего **DNS провайдера** (где зарегистрирован домен)!

Resend только показывает какие записи нужны - вы должны добавить их сами.

**Обязательные записи для отправки email:**

**A. DKIM (подпись писем)**
```
Type: TXT
Name: send._domainkey
Value: p=MIGfMA0GCSqGSIb3DQEBAQUAA4GNADCBiQKBgQC/8cafV/eI1xo...
TTL: 3600 (или Auto)
```
*Значение будет другое - скопируйте из Resend Dashboard*

**B. SPF (разрешение отправки)**
```
Type: TXT
Name: send
Value: v=spf1 include:amazonses.com ~all
TTL: 3600 (или Auto)
```

**C. DMARC (политика безопасности - опционально)**
```
Type: TXT
Name: _dmarc
Value: v=DMARC1; p=none;
TTL: 3600 (или Auto)
```

**Для получения email (опционально):**
```
Type: MX
Name: @
Value: inbound-smtp.eu-west-1.amazonaws.com
Priority: 10
TTL: 3600
```

#### 3. Где добавить DNS записи

Найдите где управляется DNS для вашего домена:

**Cloudflare:**
1. Dashboard → Websites → ваш домен
2. **DNS** → **Records**
3. **Add record** → выберите тип (TXT/MX)
4. Введите Name и Value
5. **Save**

**Namecheap:**
1. Domain List → **Manage** → **Advanced DNS**
2. **Add New Record**
3. Выберите тип, введите данные
4. Сохраните

**GoDaddy:**
1. My Products → DNS
2. **Add** → выберите тип записи
3. Введите данные
4. **Save**

**reg.ru:**
1. Домены → Управление
2. DNS-серверы и зона
3. Добавить запись
4. Сохранить

**Другие провайдеры:**
Найдите раздел "DNS Management" или "DNS Records" в панели управления доменом

#### 4. Проверка и активация

1. После добавления всех записей подождите **15-30 минут**
2. DNS распространение может занять до **48 часов** (обычно быстрее)
3. Вернитесь в Resend Dashboard → Domains
4. Resend автоматически проверит записи
5. Когда все записи обнаружены, статус станет ✅ **Verified**
6. Теперь можете использовать: `noreply@anatomia.md`

**Проверка DNS (опционально):**
```bash
# Проверить DKIM
nslookup -type=TXT send._domainkey.anatomia.md

# Проверить SPF
nslookup -type=TXT send.anatomia.md

# Проверить DMARC
nslookup -type=TXT _dmarc.anatomia.md
```

### Шаг 4: Настройка Render

1. Зайдите на https://dashboard.render.com
2. Откройте ваш сервис **anatomia-app-docker**
3. Перейдите во вкладку **Environment**
4. **Удалите старые переменные** (если есть):
   - `EMAIL_SERVICE`
   - `EMAIL_HOST`
   - `EMAIL_PORT`
   - `EMAIL_USER`
   - `EMAIL_PASSWORD`
   - `SENDGRID_API_KEY`
   - `SENDGRID_FROM_EMAIL`

5. **Добавьте новые переменные**:

   Если используете Resend email (onboarding@resend.dev):
   ```
   EMAIL_SERVICE=resend
   RESEND_API_KEY=re_ваш_скопированный_ключ
   RESEND_FROM_EMAIL=onboarding@resend.dev
   ```

   Если добавили свой домен:
   ```
   EMAIL_SERVICE=resend
   RESEND_API_KEY=re_ваш_скопированный_ключ
   RESEND_FROM_EMAIL=noreply@anatomia.app
   ```

6. Нажмите **Save Changes**
7. Render автоматически перезапустит сервер

### Шаг 5: Тестирование

1. Дождитесь завершения deployment (~5 минут)
2. Откройте ваше приложение
3. Попробуйте отправить email
4. Проверьте логи на Render - должно появиться:
   ```
   ✅ Email service initialized (Resend)
   ✅ Email sent via Resend to user@email.com: Subject
   ```

## Проверка статистики

1. В Resend Dashboard: **Emails** в левом меню
2. Увидите все отправленные emails с статусами:
   - ✅ **Delivered** - доставлено
   - 📬 **Sent** - отправлено (ещё в процессе)
   - ❌ **Failed** - ошибка

## Лимиты бесплатного плана

- **100 emails/день** - навсегда бесплатно
- После 100 emails сервис вернет ошибку, но не заблокируется
- На следующий день лимит обнуляется

Если нужно больше:
- **$20/месяц** - 50,000 emails/месяц
- Можно начать с бесплатного и апгрейднуть позже

## Troubleshooting

### Email не отправляется

1. Проверьте логи на Render:
   ```
   ✅ Email service initialized (Resend)
   ```
   Если этой строки нет - проверьте API ключ

2. Проверьте формат `RESEND_FROM_EMAIL`:
   - ✅ Правильно: `onboarding@resend.dev`
   - ✅ Правильно: `noreply@anatomia.app` (если домен верифицирован)
   - ❌ Неправильно: `Anatomia App <noreply@anatomia.app>`

### Ошибка "Domain not verified"

**Причины:**
- DNS записи не добавлены у провайдера домена
- DNS записи добавлены неправильно (опечатка в Name или Value)
- DNS еще не распространился (нужно подождать)

**Решение:**

1. **Проверьте что записи добавлены:**
   - Зайдите к вашему DNS провайдеру
   - Убедитесь что все 3 записи (DKIM, SPF, DMARC) добавлены
   - Проверьте что нет опечаток

2. **Проверьте записи командой:**
   ```bash
   nslookup -type=TXT send._domainkey.anatomia.md
   ```
   Должен вернуть значение начинающееся с `p=MIGfMA0...`

3. **Подождите распространения DNS:**
   - Обычно: 15-30 минут
   - Максимум: до 48 часов
   - Resend проверяет автоматически каждые несколько минут

4. **Временное решение:**
   Используйте `onboarding@resend.dev` пока домен не верифицируется

### Превышен лимит

Подождите до следующего дня или апгрейдните план

## Альтернативы Resend

Если Resend не подходит:

- **Brevo (Sendinblue)** - 300 emails/день бесплатно
- **Mailgun** - 5,000 emails/месяц (требует кредитку)
- **Elasticemail** - 100 emails/день бесплатно

---

## Сравнение: Resend vs SendGrid

| Параметр | Resend | SendGrid |
|----------|--------|----------|
| Бесплатный план | ✅ 100/день навсегда | ❌ Только триал |
| Регистрация | Простая | Сложная |
| API | Современный, простой | Устаревший |
| Документация | Отличная | Запутанная |
| Верификация домена | Опциональна | Обязательна |

**Вывод**: Resend - лучший выбор для небольших проектов! 🚀
